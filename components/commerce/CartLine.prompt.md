One-line: a row in the basket or checkout summary.

```jsx
<CartLine name="Ijebu garri (2kg)" vendor="Mama T Stores" price={2400} qty={2} onQty={setQty} onRemove={remove} />
```

Group lines by vendor with a small vendor header when a basket spans several sellers — that is normal on a marketplace.
