One-line: the default container for grouped content.

```jsx
<Card tone="flat">Order summary</Card>
<Card interactive raised>…</Card>
```

Cards get a border **or** a shadow, not both — `raised` drops the border. Never add a coloured left border.
