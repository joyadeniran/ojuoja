One-line: a round icon-only button for header actions, card overlays and close affordances.

```jsx
<IconButton icon="shopping-basket" variant="solid" label="Cart" count={3} />
<IconButton icon="user" variant="outline" label="Account" />
```

Always pass `label`. Icon-only buttons are always circles in this system — never squares with radius.
