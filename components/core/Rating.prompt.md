One-line: shows a vendor or product score; stars are always brand yellow.

```jsx
<Rating value={4.8} count={212} />
```

Round to one decimal. Hide the count below 5 reviews rather than showing "(2)".
