One-line: labels a thing's status; never clickable.

```jsx
<Badge tone="soft" icon="badge-check">Verified vendor</Badge>
<Badge tone="danger">-20%</Badge>
```

`danger` is reserved for price cuts and errors; `accent` for time-limited offers. Use `Tag` when the user can click it.
