One-line: paginates long product lists; circular buttons, current page filled green.

```jsx
<Pagination page={p} pages={9} onChange={setP} />
```

Prefer pagination over infinite scroll on desktop listings.
