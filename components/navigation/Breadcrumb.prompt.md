One-line: shows where in the catalogue the user is standing.

```jsx
<Breadcrumb items={[{ label: "Home", href: "/" }, { label: "Groceries" }]} />
```

Three levels maximum; the last item is plain text, not a link.
