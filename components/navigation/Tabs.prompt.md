One-line: switches between sibling views without leaving the page.

```jsx
<Tabs items={["All", "Food & Snacks", "Groceries", "Drinks"]} value={tab} onChange={setTab} />
```

Max six tabs. Beyond that, use a category grid.
