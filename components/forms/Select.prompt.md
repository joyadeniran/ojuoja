One-line: single-choice picker for sort order, area, quantity units.

```jsx
<Select label="Sort by" options={["Most popular", "Price: low to high"]} />
```

The chevron is `chevron-down` from the vendored set; don't swap in a caret glyph.
