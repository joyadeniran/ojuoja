One-line: increments a cart or basket quantity in place.

```jsx
<QuantityStepper value={qty} onChange={setQty} unit="kg" />
```

Bottom out at the minimum rather than disappearing — removal is a separate explicit action.
