One-line: a setting that applies the moment it is flipped — no Save button.

```jsx
<Switch checked={on} onChange={setOn}>Order updates on WhatsApp</Switch>
```

If the change needs confirming, use `Checkbox` instead.
