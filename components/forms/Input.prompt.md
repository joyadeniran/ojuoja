One-line: the standard labelled text input.

```jsx
<Input label="Delivery address" leadingIcon="map-pin" placeholder="Street, area" />
<Input label="Phone" shape="boxy" error="Enter an 11-digit number" />
```

Use `shape="boxy"` for multi-field checkout forms so rows read as a stack; pills for single standalone fields.
