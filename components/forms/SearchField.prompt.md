One-line: site-wide product search; the placeholder should always name real local products.

```jsx
<SearchField onSubmit={(q) => go(q)} />
```

Keep the placeholder specific ("Search for akara, suli kuli or corn"), never "Search…". Full width of the header's centre column.
