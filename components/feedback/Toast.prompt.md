One-line: confirms an action that already happened; never asks a question.

```jsx
<Toast title="Added to basket" message="2 × Ijebu garri from Mama T Stores" onClose={close} />
```

Auto-dismiss after 4s. The coloured tint lives in the icon circle only — the card stays white.
