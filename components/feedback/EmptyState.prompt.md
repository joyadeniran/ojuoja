One-line: fills a blank list with a next step, not an apology.

```jsx
<EmptyState title="Your basket is empty" message="Start with what's trending in Ikorodu today." action={<Button badgeIcon="shopping-basket">Shop Now!</Button>} />
```

Always give an action. Never use an illustration placeholder here — the icon circle is the whole graphic.
